# ati_fp_dashboard: catalog, A2C, devops and legacy registry mock data
DATABASE_URL=
# farmer_registry_db: only used by the remaining direct-SQL registry code
FARMER_DATABASE_URL=
# farmer-registry-dashboard-api base URL. Server-only; do not prefix with NEXT_PUBLIC_.
FARMER_API_BASE=http://localhost:8005
# How long registry chart data is cached before the API is called again (seconds).
REGISTRY_CACHE_TTL_SECONDS=900
